#pragma once

// Copy this file to secrets.h, then replace the placeholder values.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Development: use the VPS public IP and the uvicorn port, with TLS disabled.
// Production: use the VPS DNS name and port 443, with TLS enabled.
#define VPS_HOST "167.99.79.3"
#define VPS_PORT 8000
#define VPS_USE_TLS 0
