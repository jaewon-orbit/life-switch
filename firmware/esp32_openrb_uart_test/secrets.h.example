#pragma once

// Copy this file to secrets.h, then replace the placeholder values.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// The hostname only (no "https://"), shown when the Quick Tunnel starts.
// Quick Tunnel hostnames change every time the tunnel is restarted.
#define CLOUDFLARE_TUNNEL_HOST "YOUR-TUNNEL.trycloudflare.com"
